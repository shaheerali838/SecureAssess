# Shared Modules & Utilities

This directory contains code, constants, types, and schemas shared between the `client` and `server`.

## Planned Contents
- **Constants**: Socket event names, user roles, assessment status enums.
- **Validation**: Shared validation schemas (e.g. Zod / Joi).
- **Types / Interfaces**: Data models shared across frontend and backend.
