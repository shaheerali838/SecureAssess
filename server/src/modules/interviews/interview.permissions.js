import { PERMISSIONS } from "../../constants/permissions.js";

export const INTERVIEW_PERMISSIONS = Object.freeze({
  CREATE: PERMISSIONS.INTERVIEWS_CREATE,
  VIEW: PERMISSIONS.INTERVIEWS_VIEW,
  UPDATE: PERMISSIONS.INTERVIEWS_UPDATE,
  DELETE: PERMISSIONS.INTERVIEWS_DELETE,
  JOIN: PERMISSIONS.INTERVIEWS_JOIN,
  START: PERMISSIONS.INTERVIEWS_START,
  END: PERMISSIONS.INTERVIEWS_END,
  MANAGE_PARTICIPANTS: PERMISSIONS.INTERVIEWS_MANAGE_PARTICIPANTS,
  RECORD: PERMISSIONS.INTERVIEWS_RECORD,
  VIEW_RECORDINGS: PERMISSIONS.INTERVIEWS_VIEW_RECORDINGS,
  EVALUATE: PERMISSIONS.INTERVIEWS_EVALUATE,
  VIEW_OWN: PERMISSIONS.INTERVIEWS_VIEW_OWN,
});
